@model CreateOverheadsForm

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Расходы";
}

<article class="content">
    <div class="title-search-block">
        <div class="title-block">
            <div class="row">
                <div class="col-md-6">
                    <h3 class="title">
                        Добавление расхода
                    </h3>
                    <p class="title-description text-danger">
                        @ViewData[ViewDataKeys.ErrorMessage]
                    </p>
                </div>
            </div>
        </div>
    </div>
    <form asp-action="Add" asp-controller="Overheads" method="post" class="col-md-3">
        <div class="form-group">
            <label asp-for="Comment"></label>
            <input asp-for="Comment" type="text" class="form-control" />
            <span asp-validation-for="Comment" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Amount"></label>
            <input asp-for="Amount" type="text" class="form-control" />
            <span asp-validation-for="Amount" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="OverheadDate"></label>
            <input type="text" class="form-control js-datetimepicker" />
            <input asp-for="OverheadDate" type="hidden" />
            <span asp-validation-for="OverheadDate" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="LotId"></label>
            <select asp-for="LotId" class="form-control">
                @foreach (var item in Model.Lots)
                {
                    <option value="@item.Id">@item.IdentifierNumber (@item.CreationDate.Year)</option>
                }
            </select>
            <span asp-validation-for="LotId" class="text-danger"></span>
        </div>
        <input class="btn btn-success" type="submit" value="Сохранить" />
        <a class="btn btn-danger" asp-action="List" asp-controller="Overheads">Отмена</a>
    </form>
</article>